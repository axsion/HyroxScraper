{
  "name": "hyrox-scraper",
  "version": "28.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=/opt/render/project/.playwright npx playwright install --with-deps chromium",
    "start": "PLAYWRIGHT_BROWSERS_PATH=/opt/render/project/.playwright node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.48.2"
  }
}
